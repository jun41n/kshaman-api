Improve the share message system to avoid duplication and massive maintenance.

Current risk:
- There are many result types (MBTI etc.)
- Multiple languages exist
- Creating separate share messages per result per language would be too large and unmaintainable

I want a scalable system.

==================================================
REQUIREMENT: TEMPLATE-BASED SHARE MESSAGE
==================================================

Do NOT create separate messages for each result.

Instead:

- Create ONE share message template per language
- Inject the dynamic result title into that template

==================================================
IMPLEMENTATION
==================================================

1) Create a function:

buildShareMessage(lang, resultTitle, url)

2) Use ONE template per language only

Example:

Korean (ko):
나 결과 이거 나왔어 👀
"{RESULT_TITLE}"
너도 해보고 나랑 비교해봐
{URL}

English (en):
I got this result 👀
"{RESULT_TITLE}"
Try it and compare with me
{URL}

Japanese (ja):
私の結果はこれだった 👀
「{RESULT_TITLE}」
あなたもやって比べてみて
{URL}

Spanish (es):
Mira mi resultado 👀
"{RESULT_TITLE}"
Hazlo tú también y compáralo conmigo
{URL}

Portuguese (pt):
Olha meu resultado 👀
"{RESULT_TITLE}"
Faz o teste também e compara comigo
{URL}

Indonesian (id):
Ini hasilku 👀
"{RESULT_TITLE}"
Coba juga dan bandingkan denganku
{URL}

3) Replace {RESULT_TITLE} dynamically using the already translated result title from the current language.

4) Replace {URL} with the correct test entry URL.

==================================================
IMPORTANT RULES
==================================================

- DO NOT hardcode result-specific messages
- DO NOT duplicate logic for each result
- ONLY use language templates + dynamic insertion
- The resultTitle must already be localized (from i18n system)

==================================================
LANGUAGE DETECTION
==================================================

- Use the existing i18n or language selector system
- Automatically detect current language
- Apply the correct template

==================================================
FALLBACK
==================================================

- If language is unknown, fallback to English template

==================================================
INTEGRATION
==================================================

- Use buildShareMessage() inside:
  - shareResultComparison()
  - SNS share functions

==================================================
OUTPUT
==================================================

Show me:
1) the buildShareMessage function
2) how language is detected
3) how resultTitle is injected
4) where it is used in share logic