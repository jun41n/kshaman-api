Add SEO content sections to every MBTI result page.

Goal:
Each MBTI result page must contain **600–1000 words of SEO-friendly readable text** so that Google can index meaningful content.

Pages affected:
All MBTI result pages such as:
/results/mbti-test?result=INTJ
/results/mbti-test?result=INFP
/results/mbti-test?result=ENFP
(and all other 16 MBTI types)

Implementation instructions:

1. Add SEO content below the existing result card UI.

Do NOT remove or change the current result layout.
The SEO content must appear **under the result section**.

2. Content length requirement.

Each MBTI result page must contain **600–1000 words of text**.

Each section should contain **2–3 paragraphs** to ensure the total page content reaches the required length.

3. Required SEO sections.

Add the following headings using semantic HTML:

<h2>{TYPE} Personality Traits</h2>
<p>Describe the main personality characteristics of this MBTI type.</p>

<h2>Strengths of {TYPE}</h2>
<p>Explain common strengths such as thinking style, creativity, leadership, or empathy.</p>

<h2>Weaknesses of {TYPE}</h2>
<p>Explain possible limitations such as overthinking, emotional sensitivity, or rigidity.</p>

<h2>{TYPE} in Relationships</h2>
<p>Explain how this personality behaves in romantic relationships and friendships.</p>

<h2>Best Careers for {TYPE}</h2>
<p>Describe jobs that typically match the strengths of this personality type.</p>

<h2>Compatibility with Other MBTI Types</h2>
<p>Mention personality types that usually match well and why.</p>

4. Dynamic generation.

Use the MBTI type variable so the same component works for all 16 types.

Example headings:

INTJ Personality Traits
Strengths of INTJ
Weaknesses of INTJ

INFP Personality Traits
Strengths of INFP
Weaknesses of INFP

5. Internal linking.

At the bottom of the page add a section:

<h2>Explore Other MBTI Personality Types</h2>

Display links to the other MBTI result pages (all 16 types).
Example links:

/results/mbti-test?result=INTJ
/results/mbti-test?result=ENFP
/results/mbti-test?result=ISTP

6. SEO visibility.

Ensure the content is **rendered as normal HTML text** so Google can crawl it.
Do not hide the text behind client-side state or buttons.

7. Preserve existing functionality.

Do not break:
• existing result UI
• JSON-LD structured data
• breadcrumb schema
• current layout styling
