Fix the pet MBTI result page so it matches the layout and spacing of the other result pages exactly.

This task is ONLY about making the pet MBTI result page consistent with the other existing result pages.
Do NOT redesign the site.
Do NOT change colors, fonts, or the general visual style unless necessary to match the existing result-page pattern.
Do NOT add new features.

==================================================
GOAL
==================================================

The pet MBTI result page currently has inconsistent layout compared with the other result pages.

Current issues:
- There is an unnecessary empty purple area below the result card
- The spacing between the result card, share/save section, and bottom action buttons is inconsistent
- The overall result-page structure does not visually match the other tests

==================================================
REQUIRED FIX
==================================================

1) Make the pet MBTI result page use the SAME result layout structure as the other result pages

2) Remove the unnecessary empty purple space below the result card

3) Match the vertical spacing/padding/margins to the other result pages:
- result card area
- Compare with Friends button
- Save Image button
- SNS share area if present
- Retake Test button
- Try Another Test button

4) Make sure the pet MBTI result card container height is not artificially larger than necessary

5) Reuse the shared/common result-page component and styles if the project already has them
- Do NOT keep a separate broken custom layout for pet MBTI if a shared layout exists

==================================================
IMPORTANT
==================================================

- Preserve the existing i18n / translation system
- Do NOT break language switching
- Do NOT change result logic or scoring
- Do NOT modify unrelated test pages
- Only fix layout consistency for the pet MBTI result page

==================================================
OUTPUT REQUIRED
==================================================

Show clearly:
1) Which file renders the pet MBTI result page
2) Which shared result layout/component is used by the other result pages
3) All modified files
4) What caused the extra empty purple area
5) How you matched the pet MBTI layout to the other result pages